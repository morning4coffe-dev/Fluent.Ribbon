<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fluent="using:Fluent">

    <Style TargetType="fluent:QuickAccessToolBar">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="fluent:QuickAccessToolBar">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Main toolbar items -->
                        <StackPanel
                            x:Name="PART_ToolBarPanel"
                            Orientation="Horizontal"
                            VerticalAlignment="Center" />

                        <!-- Overflow button (chevron) -->
                        <Button
                            x:Name="PART_OverflowButton"
                            Visibility="Collapsed"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4,2"
                            MinWidth="16"
                            MinHeight="20"
                            VerticalAlignment="Center"
                            ToolTipService.ToolTip="More commands">
                            <FontIcon Glyph="&#xE70D;" FontSize="9" Foreground="{ThemeResource RibbonIconBrush}" />
                        </Button>

                        <!-- Menu dropdown button -->
                        <Button
                            x:Name="PART_MenuButton"
                            Visibility="{Binding IsMenuDropDownVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4,2"
                            MinWidth="16"
                            MinHeight="20"
                            VerticalAlignment="Center"
                            ToolTipService.ToolTip="Customize Quick Access Toolbar">
                            <FontIcon Glyph="&#xE712;" FontSize="9" Foreground="{ThemeResource RibbonIconBrush}" />
                        </Button>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
