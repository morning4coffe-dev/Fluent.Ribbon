<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fluent="using:Fluent">

    <Style TargetType="fluent:BackstageTabControl">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="ItemsPanelBackground" Value="{ThemeResource RibbonBackstageBackgroundBrush}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="fluent:BackstageTabControl">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            x:Name="PART_ItemsPanelContainer"
                            MinWidth="{TemplateBinding ItemsPanelMinWidth}"
                            Background="{TemplateBinding ItemsPanelBackground}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Button
                                x:Name="PART_BackButton"
                                MinHeight="{ThemeResource RibbonCompactTargetSize}"
                                Padding="16,0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                Background="Transparent"
                                Foreground="{ThemeResource RibbonBackstageForegroundBrush}"
                                BorderThickness="0"
                                Visibility="{Binding IsBackButtonVisible, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border
                                            x:Name="BackButtonRoot"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal" />
                                                    <VisualState x:Name="PointerOver">
                                                        <VisualState.Setters>
                                                            <Setter Target="BackButtonRoot.Background" Value="{ThemeResource RibbonBackstageBackgroundHoverBrush}" />
                                                            <Setter Target="BackButtonContent.Foreground" Value="{ThemeResource RibbonBackstageForegroundHoverBrush}" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <VisualState.Setters>
                                                            <Setter Target="BackButtonRoot.Background" Value="{ThemeResource RibbonBackstageBackgroundPressedBrush}" />
                                                            <Setter Target="BackButtonContent.Foreground" Value="{ThemeResource RibbonBackstageForegroundPressedBrush}" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                    <VisualState x:Name="Disabled">
                                                        <VisualState.Setters>
                                                            <Setter Target="BackButtonContent.Foreground" Value="{ThemeResource RibbonDisabledForegroundBrush}" />
                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>
                                            <ContentPresenter
                                                x:Name="BackButtonContent"
                                                Content="{TemplateBinding Content}"
                                                Foreground="{TemplateBinding Foreground}"
                                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <FontIcon
                                    FontSize="16"
                                    Glyph="&#xE72B;" />
                            </Button>

                            <ItemsPresenter Grid.Row="1" Margin="0,8,0,0" />
                        </Grid>

                        <Border
                            Grid.Column="1"
                            Padding="24"
                            Background="{ThemeResource RibbonContentBrush}">
                            <ContentPresenter
                                x:Name="PART_SelectedContentHost"
                                Content="{TemplateBinding SelectedContent}"
                                ContentTemplate="{TemplateBinding SelectedContentTemplate}"
                                ContentTemplateSelector="{TemplateBinding SelectedContentTemplateSelector}"
                                HorizontalContentAlignment="Stretch"
                                VerticalContentAlignment="Stretch" />
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
