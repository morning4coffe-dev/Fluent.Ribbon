<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fluent="using:Fluent">

    <fluent:IconConverter x:Key="InRibbonGalleryIconConverter" />

    <Style TargetType="fluent:InRibbonGallery">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{ThemeResource RibbonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="32" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="fluent:InRibbonGallery">
                    <Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="DisplayStates">
                                <VisualState x:Name="Inline" />
                                <VisualState x:Name="CollapsedToButton">
                                    <VisualState.Setters>
                                        <Setter Target="InlineContent.Visibility" Value="Collapsed" />
                                        <Setter Target="CollapsedContent.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                                <!-- Compact icon+chevron presentation used when the gallery is
                                     collapsed at Small size (e.g. the Quick Access Toolbar clone),
                                     mirroring WPF's horizontal Small-size gallery button. -->
                                <VisualState x:Name="CollapsedToButtonCompact">
                                    <VisualState.Setters>
                                        <Setter Target="InlineContent.Visibility" Value="Collapsed" />
                                        <Setter Target="CollapsedContent.Visibility" Value="Visible" />
                                        <Setter Target="CollapsedContent.MinWidth" Value="0" />
                                        <Setter Target="CollapsedButtonPanel.Orientation" Value="Horizontal" />
                                        <Setter Target="CollapsedButtonHeader.Visibility" Value="Collapsed" />
                                        <Setter Target="CollapsedButtonChevron.Margin" Value="3,0,0,0" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="CollapsedToButtonSimplified">
                                    <VisualState.Setters>
                                        <Setter Target="InlineContent.Visibility" Value="Collapsed" />
                                        <Setter Target="CollapsedContent.Visibility" Value="Visible" />
                                        <Setter Target="CollapsedContent.MinWidth" Value="0" />
                                        <Setter Target="CollapsedContent.MinHeight" Value="{ThemeResource RibbonCompactTargetSize}" />
                                        <Setter Target="CollapsedContent.Padding" Value="4,2" />
                                        <Setter Target="CollapsedContent.VerticalAlignment" Value="Center" />
                                        <Setter Target="CollapsedButtonPanel.Orientation" Value="Horizontal" />
                                        <Setter Target="CollapsedButtonGlyph.FontSize" Value="16" />
                                        <Setter Target="CollapsedButtonImage.Width" Value="16" />
                                        <Setter Target="CollapsedButtonImage.Height" Value="16" />
                                        <Setter Target="CollapsedButtonHeader.Margin" Value="4,0,0,0" />
                                        <Setter Target="CollapsedButtonHeader.VerticalAlignment" Value="Center" />
                                        <Setter Target="CollapsedButtonChevron.Margin" Value="4,0,0,0" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SimplifiedStates">
                                <VisualState x:Name="Classic" />
                                <VisualState x:Name="Simplified">
                                    <VisualState.Setters>
                                        <Setter Target="InlineContent.Height" Value="NaN" />
                                        <Setter Target="InlineContent.MinHeight" Value="0" />
                                        <Setter Target="UpButtonRow.Height" Value="10" />
                                        <Setter Target="DownButtonRow.Height" Value="10" />
                                        <Setter Target="PART_UpButton.MinHeight" Value="10" />
                                        <Setter Target="PART_DownButton.MinHeight" Value="10" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <!-- Inline Gallery Display -->
                        <Border
                            x:Name="InlineContent"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            Height="{TemplateBinding GalleryPanelContainerHeight}"
                            MinHeight="{ThemeResource RibbonInlineGalleryMinHeight}"
                            MinWidth="80">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- Gallery items area -->
                                <ScrollViewer
                                    x:Name="PART_ScrollViewer"
                                    HorizontalScrollBarVisibility="Disabled"
                                    VerticalScrollBarVisibility="Hidden"
                                    HorizontalScrollMode="Disabled"
                                    VerticalScrollMode="Enabled">
                                    <fluent:UniformItemsPanel
                                        x:Name="PART_GalleryPanel" />
                                </ScrollViewer>

                                <!-- WPF uses a 17px scroll-bar strip with the expand button
                                     occupying the bottom cell. Keep the same footprint while
                                     retaining platform-native monochrome Fluent glyphs. -->
                                <Grid
                                    x:Name="PART_ActionStrip"
                                    Grid.Column="1"
                                    Width="{ThemeResource RibbonCompactTargetSize}"
                                    Background="{ThemeResource RibbonBackgroundBrush}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition
                                            x:Name="UpButtonRow"
                                            Height="{ThemeResource RibbonCompactTargetSize}" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition
                                            x:Name="DownButtonRow"
                                            Height="{ThemeResource RibbonCompactTargetSize}" />
                                        <RowDefinition Height="{ThemeResource RibbonCompactTargetSize}" />
                                    </Grid.RowDefinitions>

                                    <Button
                                        x:Name="PART_UpButton"
                                        Grid.Row="0"
                                        Background="Transparent"
                                        Foreground="{ThemeResource RibbonIconBrush}"
                                        BorderThickness="0"
                                        CornerRadius="0"
                                        Padding="0"
                                        MinWidth="{ThemeResource RibbonCompactTargetSize}"
                                        MinHeight="{ThemeResource RibbonCompactTargetSize}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        AutomationProperties.AutomationId="InRibbonGalleryUpButton">
                                        <FontIcon
                                            Glyph="&#xE70E;"
                                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                            FontSize="8" />
                                    </Button>

                                    <Button
                                        x:Name="PART_DownButton"
                                        Grid.Row="2"
                                        Background="Transparent"
                                        Foreground="{ThemeResource RibbonIconBrush}"
                                        BorderThickness="0"
                                        CornerRadius="0"
                                        Padding="0"
                                        MinWidth="{ThemeResource RibbonCompactTargetSize}"
                                        MinHeight="{ThemeResource RibbonCompactTargetSize}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        AutomationProperties.AutomationId="InRibbonGalleryDownButton">
                                        <FontIcon
                                            Glyph="&#xE70D;"
                                            FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                            FontSize="8" />
                                    </Button>

                                    <Rectangle
                                        Grid.Row="3"
                                        Height="1"
                                        Fill="{ThemeResource RibbonBorderBrush}"
                                        VerticalAlignment="Top"
                                        HorizontalAlignment="Stretch" />

                                    <Button
                                        x:Name="PART_ExpandButton"
                                        Grid.Row="3"
                                        Background="Transparent"
                                        Foreground="{ThemeResource RibbonIconBrush}"
                                        BorderThickness="0"
                                        CornerRadius="0"
                                        Padding="0"
                                        MinWidth="{ThemeResource RibbonCompactTargetSize}"
                                        MinHeight="{ThemeResource RibbonCompactTargetSize}"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        AutomationProperties.AutomationId="InRibbonGalleryExpandButton"
                                        Content="{Binding ExpandButtonContent, RelativeSource={RelativeSource TemplatedParent}}"
                                        ContentTemplate="{Binding ExpandButtonContentTemplate, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Grid>
                            </Grid>
                        </Border>

                        <!-- Collapsed to button display -->
                        <Button
                            x:Name="CollapsedContent"
                            Visibility="Collapsed"
                            Background="Transparent"
                            Foreground="{ThemeResource RibbonIconBrush}"
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}"
                            MinWidth="56"
                            MinHeight="{ThemeResource RibbonCompactTargetSize}"
                            AutomationProperties.AutomationId="InRibbonGalleryCollapsedButton"
                            VerticalAlignment="Stretch">
                            <StackPanel x:Name="CollapsedButtonPanel" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- Icon glyph or image -->
                                <FontIcon
                                    x:Name="CollapsedButtonGlyph"
                                    Glyph="{Binding IconGlyph, RelativeSource={RelativeSource TemplatedParent}}"
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="{TemplateBinding FontSize}"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IconGlyph, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EmptyStringToCollapsedConverter}}" />

                                <Image
                                    x:Name="CollapsedButtonImage"
                                    Source="{Binding LargeIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource InRibbonGalleryIconConverter}}"
                                    Width="32"
                                    Height="32"
                                    Stretch="Uniform"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding LargeIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}}" />

                                <ContentPresenter
                                    x:Name="CollapsedButtonHeader"
                                    Content="{TemplateBinding Header}"
                                    ContentTemplate="{TemplateBinding HeaderTemplate}"
                                    ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                    HorizontalAlignment="Center"
                                    Margin="0,2,0,0" />

                                <FontIcon
                                    x:Name="CollapsedButtonChevron"
                                    Glyph="&#xE70D;"
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="8"
                                    VerticalAlignment="Center"
                                    Margin="0,2,0,0" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
