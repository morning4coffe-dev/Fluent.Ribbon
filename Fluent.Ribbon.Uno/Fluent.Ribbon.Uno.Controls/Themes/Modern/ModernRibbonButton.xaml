<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Fluent.Converters"
    xmlns:fluent="using:Fluent"
    xmlns:modernControls="using:Fluent.Modern.Controls">

    <converters:BoolToVisibilityConverter x:Key="ModernBoolToVisibilityConverter" />
    <converters:BoolToVisibilityConverter x:Key="ModernInverseBoolToVisibilityConverter" Invert="True" />

    <Style TargetType="modernControls:ModernRibbonButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="{ThemeResource RibbonButtonPadding}" />
        <Setter Property="MinHeight" Value="{ThemeResource RibbonControlMinHeight}" />
        <Setter Property="Foreground" Value="{ThemeResource RibbonIconBrush}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="modernControls:ModernRibbonButton">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3"
                        MinWidth="44"
                        Padding="{TemplateBinding Padding}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonHoverBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonBorderBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonPressedBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonAccentBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ContentPanel"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.5"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SizeStates">
                                <VisualState x:Name="Large">
                                    <VisualState.Setters>
                                        <Setter Target="ContentPanel.Orientation" Value="Vertical" />
                                        <Setter Target="ContentPanel.Spacing" Value="0" />
                                        <Setter Target="PART_ModernIcon.IconSize" Value="Large" />
                                        <Setter Target="PART_GlyphIcon.FontSize" Value="28" />
                                        <Setter Target="PART_Icon.Width" Value="32" />
                                        <Setter Target="PART_Icon.Height" Value="32" />
                                        <Setter Target="PART_Label.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Medium">
                                    <VisualState.Setters>
                                        <Setter Target="ContentPanel.Orientation" Value="Horizontal" />
                                        <Setter Target="ContentPanel.Spacing" Value="4" />
                                        <Setter Target="PART_ModernIcon.IconSize" Value="Small" />
                                        <Setter Target="PART_GlyphIcon.FontSize" Value="16" />
                                        <Setter Target="PART_Icon.Width" Value="16" />
                                        <Setter Target="PART_Icon.Height" Value="16" />
                                        <Setter Target="PART_Icon.Margin" Value="0" />
                                        <Setter Target="PART_GlyphIcon.Margin" Value="0" />
                                        <Setter Target="PART_GlyphIcon.VerticalAlignment" Value="Center" />
                                        <Setter Target="PART_Label.Visibility" Value="Visible" />
                                        <Setter Target="PART_Label.TextWrapping" Value="NoWrap" />
                                        <Setter Target="PART_Label.MaxWidth" Value="180" />
                                        <Setter Target="PART_Label.TextAlignment" Value="Left" />
                                        <Setter Target="PART_Label.VerticalAlignment" Value="Center" />
                                        <Setter Target="PART_Label.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Small">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.MinWidth" Value="44" />
                                        <Setter Target="ContentPanel.Orientation" Value="Horizontal" />
                                        <Setter Target="ContentPanel.Spacing" Value="0" />
                                        <Setter Target="PART_ModernIcon.IconSize" Value="Small" />
                                        <Setter Target="PART_GlyphIcon.FontSize" Value="16" />
                                        <Setter Target="PART_Icon.Width" Value="16" />
                                        <Setter Target="PART_Icon.Height" Value="16" />
                                        <Setter Target="PART_Label.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <StackPanel x:Name="ContentPanel" Orientation="Vertical" HorizontalAlignment="Center">
                            <Grid x:Name="IconSlot">
                                <modernControls:ModernIconPresenter
                                    x:Name="PART_ModernIcon"
                                    Source="{TemplateBinding CurrentIconSource}"
                                    Visibility="{Binding HasIconSource, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ModernBoolToVisibilityConverter}, FallbackValue=Collapsed}" />
                                <Grid Visibility="{Binding HasIconSource, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ModernInverseBoolToVisibilityConverter}, FallbackValue=Visible}">
                                    <FontIcon
                                        x:Name="PART_GlyphIcon"
                                        Glyph="{TemplateBinding IconGlyph}"
                                        FontSize="28"
                                        Foreground="{TemplateBinding Foreground}"
                                        Visibility="{Binding IconGlyph, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource EmptyStringToCollapsedConverter}, FallbackValue=Collapsed}" />
                                    <Image
                                        x:Name="PART_Icon"
                                        Width="32"
                                        Height="32"
                                        Source="{TemplateBinding CurrentIcon}"
                                        Stretch="Uniform"
                                        Visibility="{Binding CurrentIcon, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}, FallbackValue=Collapsed}" />
                                </Grid>
                            </Grid>
                            <TextBlock
                                x:Name="PART_Label"
                                Text="{TemplateBinding Header}"
                                Foreground="{ThemeResource RibbonTextBrush}"
                                TextAlignment="Center"
                                TextWrapping="Wrap"
                                MaxLines="2"
                                TextTrimming="CharacterEllipsis"
                                MaxWidth="60"
                                Margin="0,2,0,0"
                                FontSize="11" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
