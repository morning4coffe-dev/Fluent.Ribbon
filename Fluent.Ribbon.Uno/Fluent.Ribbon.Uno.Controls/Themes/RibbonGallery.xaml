<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fluent="using:Fluent">

    <Style TargetType="fluent:RibbonGallery">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{ThemeResource RibbonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="fluent:RibbonGallery">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Filter Bar (visible when HasFilter is true) -->
                            <Border
                                x:Name="PART_FilterBar"
                                Background="{ThemeResource RibbonBackgroundBrush}"
                                BorderBrush="{ThemeResource RibbonBorderBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="4,2"
                                Visibility="{Binding HasFilter, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock
                                        Text="Filter:"
                                        FontSize="11"
                                        Foreground="{ThemeResource RibbonSecondaryTextBrush}"
                                        VerticalAlignment="Center"
                                        Margin="4,0" />
                                    <ItemsRepeater
                                        x:Name="PART_FilterButtons"
                                        VerticalAlignment="Center">
                                        <ItemsRepeater.Layout>
                                            <StackLayout Orientation="Horizontal" Spacing="2" />
                                        </ItemsRepeater.Layout>
                                    </ItemsRepeater>
                                </StackPanel>
                            </Border>

                            <!-- Header -->
                            <ContentPresenter
                                Grid.Row="1"
                                Content="{TemplateBinding Header}"
                                Margin="8,4,8,2"
                                Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToCollapsedConverter}, FallbackValue=Collapsed}">
                                <ContentPresenter.ContentTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Text="{Binding}"
                                            FontWeight="SemiBold"
                                            FontSize="11"
                                            Foreground="{ThemeResource RibbonSecondaryTextBrush}" />
                                    </DataTemplate>
                                </ContentPresenter.ContentTemplate>
                            </ContentPresenter>

                            <!-- Items (grouped or flat) -->
                            <ScrollViewer
                                x:Name="PART_ScrollViewer"
                                Grid.Row="2"
                                HorizontalScrollBarVisibility="Disabled"
                                VerticalScrollBarVisibility="Auto"
                                Padding="4">
                                <StackPanel x:Name="PART_GroupedPanel">
                                    <!-- When grouped: group headers + item grids are built here programmatically -->
                                    <!-- When not grouped: ItemsRepeater is used directly -->
                                    <ItemsRepeater
                                        x:Name="PART_ItemsPanel"
                                        ItemsSource="{TemplateBinding Items}">
                                        <ItemsRepeater.Layout>
                                            <UniformGridLayout
                                                MinItemWidth="{Binding ItemWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                                MinItemHeight="{Binding ItemHeight, RelativeSource={RelativeSource TemplatedParent}}"
                                                MinColumnSpacing="2"
                                                MinRowSpacing="2" />
                                        </ItemsRepeater.Layout>
                                    </ItemsRepeater>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="fluent:RibbonGalleryItem">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="fluent:RibbonGalleryItem">
                    <Border
                        x:Name="RootBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3"
                        Padding="4">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonHoverBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonBorderBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource RibbonPressedBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Selected">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.BorderBrush" Value="{ThemeResource RibbonAccentBrush}" />
                                        <Setter Target="RootBorder.Background" Value="{ThemeResource RibbonPressedBrush}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <ContentPresenter
                            Content="{TemplateBinding Content}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="fluent:GalleryGroupContainer">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="fluent:GalleryGroupContainer">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ContentPresenter
                            x:Name="PART_Header"
                            Content="{TemplateBinding Header}"
                            Margin="4,2,4,4"
                            Visibility="{Binding IsHeadered, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <fluent:GalleryPanel
                            x:Name="PART_ItemsPanel"
                            Grid.Row="1"
                            Orientation="{TemplateBinding Orientation}"
                            ItemWidth="{TemplateBinding ItemWidth}"
                            ItemHeight="{TemplateBinding ItemHeight}"
                            MinItemsInRow="{TemplateBinding MinItemsInRow}"
                            MaxItemsInRow="{TemplateBinding MaxItemsInRow}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
